#!/bin/bash
#Clears the all caches

for node in $(cat /usr/workspace/etc/nodes);
do
    echo "Cleaning node $node"
    ssh root@$node /usr/workspace/bin/vw-cache-clear
done

echo "Cleaning global cache"
rm /var/workspace/image_cache_db

echo "Cleaning events"
rm /var/workspace/events/*